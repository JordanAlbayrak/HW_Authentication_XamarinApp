<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="multyPage_XamarinAssign.HomePage">

    <StackLayout>
        <Label Text="Welcome to Home page!"  
        VerticalOptions="Center"
        HorizontalOptions="Center"
        FontSize="Large"/>
        
        <Label Text="{Binding username}"
        FontSize="Large"/>

        <!--<CollectionView x:Name="collectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Grid Padding="10" RowDefinitions="Auto, *">
                            <Label x:Name="userName"  Grid.Row="0" Grid.Column="0" Text="{Binding username}" FontSize="Medium"/>
                            
                            <Label Grid.Row="0" Grid.Column="5" Text="{Binding ID}" TextColor="Silver" FontSize="Small"/>

                        </Grid>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>-->
        <!--</CollectionView> -->
    </StackLayout>

</ContentPage>
